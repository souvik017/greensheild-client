# Backend API base URL (includes the /api prefix used by the server routes).
# Leave empty to fall back to the relative '/api' path (Vite dev proxy / same-origin hosting).
VITE_API_URL=https://greensheild-server.onrender.com/api